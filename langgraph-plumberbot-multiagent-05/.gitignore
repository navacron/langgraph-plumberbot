.env
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
dist/
build/
.DS_Store

# Runtime checkpoint DB (not business data — not committed)
/tmp/
plumberbot_checkpoints.db
*_checkpoints.db

# Embeddings cache — ignored by default so local builds don't pollute git.
# To ship a pre-computed cache: git add -f embeddings_cache.pkl
embeddings_cache.pkl
